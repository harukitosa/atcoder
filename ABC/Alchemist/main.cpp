#include<bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0);
    ios::sync_with_stdio(false);
    int n;
    cin >> n;
    vector<double> vc;
    for(int i = 0;i < n;i++) {
        double x;
        cin >> x;
        vc.push_back(x);
    }

    
}
