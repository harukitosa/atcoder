#include <bits/stdc++.h>
using namespace std;

int main() {
    int m, d, n, b;
    cin >> m;
    cin >> d;
    cin >> n;
    cin >> b;
    if(b == 1) {
        cout << 1 << '\n';
    } else {
        cout << 0 << '\n';
    }
}