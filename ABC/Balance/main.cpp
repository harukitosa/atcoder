#include<bits/stdc++.h>
using namespace std;
int main() {
    cin.tie(0);
    ios::sync_with_stdio(false);
    int n;
    vector<int> vc;
    cin >> n;
    for(int i = 0;i < n;i++) {
        int x; cin >> x;
        vc.push_back(x);
    }
    for(int i = 0;i < n;i++) {
        for(int j = 0;j < n;j++) {
            
        }
    }
    cout << flag << endl;
}